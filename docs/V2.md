# V2 — Architecture & Modernization

## What Changed

### 1. Dead Code Removal

All original root-level files were superseded by the `poker/` package and have been deleted:

| File | Lines | Reason |
|---|---|---|
| `misc.py` | 479 | Old support library (cards, ranges, evaluation, equity, table rendering) |
| `poker_cli.py` | 311 | Monolithic CLI game, superseded by `poker/` package + GUI |
| `poker_gui.py` | 523 | Monolithic PySimpleGUI game, superseded by `ui/` (Flet) |
| `player.py` | 14 | Old Player class, superseded by `poker/player.py` (dataclass) |
| `main.py` | 6 | Stub, replaced with actual entry point |
| `hv.json` | 48 MB | Pre-computed hand value table, superseded by algorithmic + pokerkit evaluation |
| `requirements.txt` | 288 | Frozen environment cruft, superseded by `pyproject.toml` + `uv.lock` |

**Total removed: ~1,600 lines of Python + 48 MB JSON.**

### 2. Python 3.13 Native

- Removed `from __future__ import annotations` from all `poker/` modules (PEP 604 unions, deferred evaluation, and all 3.13 features are now native).
- Added `strict=True` to all `zip()` calls in `game.py` for safety.
- `pyproject.toml` targets `>=3.13`.

### 3. Architecture

```
main.py                          ← single entry point
 └─ ui.app.main()
     ├─ FletGUI (ui/app.py)     ← implements GameUI protocol (Flet)
     │   └─ runs game loop      ← daemon thread, sync via threading.Event
     └─ TexasHoldem (poker/game.py) ← UI-agnostic engine
         ├─ evaluation.py       ← backed by pokerkit StandardHighHand
         ├─ equity.py           ← Monte Carlo equity simulation
         ├─ ranges.py           ← pre-flop hand rankings
         ├─ cards.py            ← card primitives, 52-card deck
         └─ player.py           ← Player dataclass
```

**Threading model:** The game engine runs synchronously in a background daemon thread. When the engine calls `request_bet()`, the GUI updates controls on the Flet page, calls `page.update()`, then blocks on a `threading.Event`. The user's button click (Check/Call, Raise, Fold) sets the event and unblocks the thread, returning the bet value.

### 4. Hand Evaluation — Pokerkit Integration

`evaluation.py` was rewritten to use `pokerkit.StandardHighHand` instead of a custom algorithmic evaluator.

| Before | After |
|---|---|
| Custom `Counter`-based 5-card evaluation | `StandardHighHand` with `StandardLookup` |
| Manual `C(n,5)` max over combinations | `max(combinations(cards, 5), key=StandardHighHand)` |
| Hand category as first tuple element | `value.entry.label` → `Label.STRAIGHT_FLUSH` etc. |
| `HandValue = tuple[int, ...]` | `HandValue = StandardHighHand` (directly comparable) |

Benefits:
- Battle-tested evaluation from a dedicated poker library (forked from `uoftcprg/pokerkit`)
- Built-in hand descriptions (e.g. `"Full house"`, `"Straight flush"`)
- Unique ranking indices via `entry.index`
- Eliminates 48MB hv.json dependency

### 5. GUI — Flet (Cross-Platform Desktop)

The old PySimpleGUI + matplotlib + PIL GUI was replaced with pure tkinter (~250 lines), which was then replaced with a **Flet** implementation (~350 lines).

**Layout:**
- **Player area** — wrapped row of player cards showing names, chip counts, and hole cards (card backs for inactive players, revealed for the active player)
- **Board area** — 5 community card slots with suit-colored text (♠♥♦♣)
- **Pot display** — live pot size
- **Hand evaluation** — gold label showing pokerkit's hand name at showdown (e.g. "Full house")
- **Action buttons** — Check/Call, Raise (with Flet Slider for amount), Fold
- **Game log** — scrollable monospace log (beige parchment background) showing all events

**Integration pattern:** The `GameUI` protocol's `request_bet()` uses `threading.Event` to block the game thread without freezing the Flet event loop, enabling the synchronous game engine to drive an event-driven GUI.

**Key Flet conventions used:**
- `ft.run(main)` entry point with `page: ft.Page`
- `SafeArea` as root wrapper
- `Container` for backgrounds, cards, and borders
- `Column` / `Row` for layout composition
- `Button` with `on_click` event handlers
- `Slider` with `min`/`max`/`value` for raise amount
- `Text` for all labels (themed colors, sizes, weights)
- Automatic `page.update()` at end of event handlers
- Manual `page.update()` from background game thread

### 6. Entry Point — Single File

```
python main.py
```

or

```
flet run main.py
```

Supports optional arguments: `-n/--players`, `-b/--buy-in`, `--big-blind`, `--seed`, `--names`.

The console_scripts entry point is registered as `poker = "ui.app:main"`.

### 7. Bug Fixes

| Issue | Fix |
|---|---|
| `test_five_card_category_counts_on_reduced_deck` wrong assertion | `5*4*16` → `5*16` (was 320, correct is 80) |
| `test_folding_awards_pot_to_last_player` infinite loop | Replaced with 2-player `AlwaysCallUI` test that converges |
| Ruff `B905` (zip without strict) | Added `strict=True` to all zip calls |
| Ruff `F401`/`I001`/`SIM105` | Cleaned up imports, unused variables, try/except patterns |
| `category` import in `game.py` | Removed unused import after showdown format change |

### 8. Code Metrics

| Metric | Before | After |
|---|---|---|
| Python files | 16 | 13 (9 poker/ + 2 ui/ + main.py + tests/) |
| Python lines | ~3,400 | ~1,350 |
| External deps | PySimpleGUI, numpy, matplotlib, PIL, ColorPrint | flet, pokerkit (only) |
| JSON data | 48 MB (hv.json) | 0 |
| Tests | N/A | 21, all passing |
| Ruff | N/A | All checks passing |
| GUI framework | PySimpleGUI → Tkinter → Flet | Flet (cross-platform desktop) |
